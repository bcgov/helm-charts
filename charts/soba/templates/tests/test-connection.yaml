apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "soba.fullname" . }}-test-connection"
  labels:
    {{- include "soba.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "soba.fullname" . }}:{{ .Values.fe_service.port }}']
  restartPolicy: Never
